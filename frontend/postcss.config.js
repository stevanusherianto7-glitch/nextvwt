/**
 * PostCSS config untuk NextVWT — Tailwind CSS v4
 *
 * Tailwind v4 menggunakan @tailwindcss/vite plugin (bukan PostCSS plugin).
 * File ini SENGAJA dikosongkan untuk mencegah global Tailwind v3 yang
 * terinstall di C:\Users\ASUS\node_modules\tailwindcss dari diterapkan
 * secara otomatis oleh Vite via parent directory PostCSS config discovery.
 *
 * Jangan tambahkan 'tailwindcss' ke sini — sudah dihandle oleh vite.config.ts.
 */
export default {
  plugins: {},
};
